databases:
  - name: veggie-store-db
    plan: free
    databaseName: veggie_store
    user: veggie

services:
  - type: web
    name: veggie-store-api
    plan: free
    runtime: node
    rootDir: server
    buildCommand: npm install && npx prisma generate && npm run build && npx prisma migrate deploy && npx prisma db seed
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: veggie-store-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        value: https://client-opal-kappa.vercel.app
